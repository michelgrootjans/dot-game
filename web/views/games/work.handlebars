<div class="container" data-game-id="{{work.gameId}}">
    <h1>{{work.work.title}}</h1>

    <div class="row">
        <progress value="" id="progressbar"></progress>
    </div>

    <div class="row" id="work-container">
        <div id="inbox" class="wait-column column" data-column-id="{{work.inbox.columnId}}">
            <h2>Inbox</h2>
            <div class="tasks"></div>
        </div>

        <div id="workspace" class="column" data-column-id="{{work.work.columnId}}" data-column-difficulty="{{work.work.difficulty}}">
            <h2>Workspace</h2>
            <div id="questions"></div>
            <div class="tasks"></div>
        </div>
    </div>

</div>

{{>cardtemplate }}